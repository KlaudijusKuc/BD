import { defineEventHandler } from 'h3'

export default defineEventHandler((event) => {
  // This is a placeholder middleware file to satisfy Nuxt's requirements
  // The actual API handling is done in the server/api directory
  return
}) 